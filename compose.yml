# Exchange Services - Environment-based Configuration
# Usage: cp .env.example .env && docker compose up --build
#
# This stack is self-contained: it starts a PostgreSQL database so the
# services can be tested end-to-end with a single `docker compose up --build`.
#
# Note: Audit Service is optional and lives in its own repo now:
#       https://github.com/LSFLK/argus
#       Services will function normally without audit-service when
#       AUDIT_SERVICE_URL is not set or ENABLE_AUDIT=false.

services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres-${ENVIRONMENT:-local}
    environment:
      - POSTGRES_USER=${DB_USER:-exchange}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-exchange}
      # Just the server's initial connection database — postgres requires one.
      # The actual per-service databases (pdp, consent_engine, orchestration_engine,
      # audit) are hardcoded and created separately by init-db.sql on first startup.
      - POSTGRES_DB=postgres
    ports:
      - "${PORT_DB:-5432}:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-exchange} ${DB_NAME:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
    restart: unless-stopped
    networks:
      - openndx-network

  # -------------------------------------------------------------------
  # 2. Identity Provider Core Services (Thunder Stack)
  # -------------------------------------------------------------------
  # Stage A: Seed static database schemas from container image layer
  thunderid-db-init:
    image: ghcr.io/thunder-id/thunderid:1.0.1
    container_name: ndx-thunderid-db-init
    # Seed only when the volume is empty. An unconditional copy would overwrite
    # configdb.db on every `up`, silently reverting the bootstrap and seed.
    command: sh -c '[ -f /data/configdb.db ] || cp -r /opt/thunderid/database/* /data/'
    volumes:
      - thunderid-db:/data
    restart: "no"

  # Stage B: generate key material, then import the declarative bootstrap resources
  # (the image's built-in defaults plus our own user/resource/application files below).
  thunderid-setup:
    image: ghcr.io/thunder-id/thunderid:1.0.1
    container_name: ndx-thunderid-setup
    command: ./setup.sh
    networks:
      - openndx-network
    environment:
      - PUBLIC_URL=${IDP_PUBLIC_URL:-https://localhost:8090}
      - CONSENT_PORTAL_URL=${CONSENT_PORTAL_URL:-http://localhost:5173}
      - DATA_CONSUMER_CLIENT_SECRET=${DATA_CONSUMER_CLIENT_SECRET:-1234}
      - ADMIN_PASSWORD=${IDP_ADMIN_PASSWORD:-1234}
    volumes:
      - thunderid-db:/opt/thunderid/database
      # Generated here, so must outlive this container.
      - thunderid-certs:/opt/thunderid/config/certs
      - thunderid-secrets:/opt/thunderid/config/secrets
      - ./thunderid/deployment.yaml:/opt/thunderid/deployment.yaml
      - ./thunderid/bootstrap/users.yaml:/opt/thunderid/bootstrap/02-users.yaml
      - ./thunderid/bootstrap/resource.yaml:/opt/thunderid/bootstrap/03-resource.yaml
      - ./thunderid/bootstrap/application.yaml:/opt/thunderid/bootstrap/04-application.yaml
      - ./thunderid/bootstrap/cors.yaml:/opt/thunderid/bootstrap/05-cors.yaml
    depends_on:
      thunderid-db-init:
        condition: service_completed_successfully
    restart: "no"

  # Stage C: Boot active server instances listening for authentication queries
  thunderid:
    image: ghcr.io/thunder-id/thunderid:1.0.1
    container_name: ndx-thunderid
    networks:
      - openndx-network
    ports:
      - "8090:8090"
    volumes:
      - thunderid-db:/opt/thunderid/database
      # Read-only: consume the key material Stage B generated, never regenerate it.
      - thunderid-certs:/opt/thunderid/config/certs:ro
      - thunderid-secrets:/opt/thunderid/config/secrets:ro
      - ./thunderid/deployment.yaml:/opt/thunderid/deployment.yaml
    healthcheck:
      test: [ "CMD-SHELL", "curl -fsk -o /dev/null https://localhost:8090/health/readiness || exit 1" ]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 15s
    depends_on:
      thunderid-setup:
        condition: service_completed_successfully

  policy-decision-point:
    build:
      context: .
      dockerfile: cmd/pdp/Dockerfile
      args:
        BUILD_VERSION: ${BUILD_VERSION:-dev}
        BUILD_TIME: ${BUILD_TIME:-2025-09-09T16:30:00Z}
        GIT_COMMIT: ${GIT_COMMIT:-local}
    container_name: pdp-${ENVIRONMENT:-local}
    ports:
      - "${PORT_PDP:-8082}:8082"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-local}
      - PORT=8082
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - LOG_FORMAT=${LOG_FORMAT:-text}
      - SERVICE_NAME=policy-decision-point
      - OTEL_METRICS_EXPORTER=${OTEL_METRICS_EXPORTER:-prometheus}
      # Database configuration
      - DB_HOST=${DB_HOST:-postgres}
      - DB_PORT=${DB_PORT:-5432}
      - DB_USERNAME=${DB_USER:-exchange}
      - DB_PASSWORD=${DB_PASSWORD:-exchange}
      - DB_NAME=${DB_NAME_PDP:-pdp}
      - DB_SSLMODE=${DB_SSLMODE:-disable}
      # IDP_AUDIENCE here is the OE resource server's identifier — kept separate
      # from consent-engine's CONSENT_ENGINE_AUDIENCE, see resource.yaml.
      - IDP_ISSUER=${IDP_PUBLIC_URL:-}
      - IDP_AUDIENCE=${IDP_AUDIENCE:-}
      - IDP_JWKS_URL=${IDP_JWKS_URL:-}
      - RUN_MIGRATION=${RUN_MIGRATION:-true}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - openndx-network

  consent-engine:
    build:
      context: .
      dockerfile: cmd/ce/Dockerfile
      args:
        BUILD_VERSION: ${BUILD_VERSION:-dev}
        BUILD_TIME: ${BUILD_TIME:-2025-09-09T16:30:00Z}
        GIT_COMMIT: ${GIT_COMMIT:-local}
    container_name: ce-${ENVIRONMENT:-local}
    ports:
      - "${PORT_CE:-8081}:8081"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-local}
      - PORT=8081
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - LOG_FORMAT=${LOG_FORMAT:-text}
      - CONSENT_PORTAL_URL=${CONSENT_PORTAL_URL:-http://localhost:5173}
      # IDP configuration (JWT verification for portal endpoints)
      # Deliberately its own audience, distinct from OE/PDP's IDP_AUDIENCE — see
      # thunderid/bootstrap/resource.yaml for why sharing one would let a token
      # minted for one API be replayed against the other.
      - IDP_ISSUER=${IDP_PUBLIC_URL:-}
      - IDP_AUDIENCE=${CONSENT_ENGINE_AUDIENCE:-}
      - IDP_CLIENT_ID=${CONSENT_PORTAL_CLIENT_ID:-}
      - IDP_JWKS_URL=${IDP_JWKS_URL:-}
      - IDP_JWKS_INSECURE_SKIP_VERIFY=${IDP_JWKS_INSECURE_SKIP_VERIFY:-false}
      # Database configuration
      - DB_HOST=${DB_HOST:-postgres}
      - DB_PORT=${DB_PORT:-5432}
      - DB_USERNAME=${DB_USER:-exchange}
      - DB_PASSWORD=${DB_PASSWORD:-exchange}
      - DB_NAME=${DB_NAME_CE:-consent_engine}
      - DB_SSLMODE=${DB_SSLMODE:-disable}
      - RUN_MIGRATION=${RUN_MIGRATION:-true}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - openndx-network

  orchestration-engine:
    build:
      context: .
      dockerfile: cmd/oe/Dockerfile
      args:
        BUILD_VERSION: ${BUILD_VERSION:-dev}
        BUILD_TIME: ${BUILD_TIME:-2025-09-09T16:30:00Z}
        GIT_COMMIT: ${GIT_COMMIT:-local}
    container_name: oe-${ENVIRONMENT:-local}
    ports:
      - "${PORT_OE:-4000}:4000"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-local}
      - PORT=4000
      - LOG_LEVEL=${LOG_LEVEL:-info}
      - LOG_FORMAT=${LOG_FORMAT:-text}
      - SERVICE_NAME=orchestration-engine
      - OTEL_METRICS_EXPORTER=${OTEL_METRICS_EXPORTER:-prometheus}
      # Database configuration
      - DB_HOST=${DB_HOST:-postgres}
      - DB_PORT=${DB_PORT:-5432}
      - DB_USERNAME=${DB_USER:-exchange}
      - DB_PASSWORD=${DB_PASSWORD:-exchange}
      - DB_NAME=${DB_NAME_OE:-orchestration_engine}
      - DB_SSLMODE=${DB_SSLMODE:-disable}
      - AUDIT_SERVICE_URL=${AUDIT_SERVICE_URL:-http://audit-service:3001}
      - ENABLE_AUDIT=${ENABLE_AUDIT:-false}
      - ARGUS_API_KEY=${ARGUS_API_KEY:-test-audit-token}
    volumes:
      - ./cmd/oe/config.docker.json:/app/config.json:ro
    extra_hosts:
      # Lets the container reach mock providers running on the host
      # (resolves host.docker.internal on Linux too).
      - "host.docker.internal:host-gateway"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://localhost:4000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - openndx-network

  audit-service:
    build:
      context: https://github.com/LSFLK/argus.git#v1.0.1
      dockerfile: Dockerfile
    container_name: audit-service-${ENVIRONMENT:-local}
    ports:
      - "${PORT_AUDIT:-3001}:3001"
    environment:
      - ENVIRONMENT=${ENVIRONMENT:-local}
      - DB_TYPE=postgres
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USERNAME=${DB_USER:-exchange}
      - DB_PASSWORD=${DB_PASSWORD:-exchange}
      - DB_NAME=audit
      - DB_SSLMODE=${DB_SSLMODE:-disable}
      - PORT=3001
      - ARGUS_API_KEY=${ARGUS_API_KEY:-test-audit-token}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - openndx-network

volumes:
  postgres-data:
    driver: local
  thunderid-db:
    driver: local
  # Key material is generated per deployment, not shipped in the image. Without these
  # volumes the server regenerates a different AES key and cannot decrypt the client
  # secrets already stored in configdb.db.
  thunderid-certs:
    driver: local
  thunderid-secrets:
    driver: local

networks:
  openndx-network:
    name: openndx-network
